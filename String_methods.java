//27-32 class videos on string and array

/* 1.name.toLowerCase - used to convert to lowercase 
   2.name.ToUpperCase - used to convert to uppercase
   3.name.length - used to give the length of the given string
   4.name.trim - used to trim the extra spaces at the beginning and end of a string
   5.name.concat - used to combine two or more strings
   6.name.charAt(Index) - it gives the characater at the given index
   7.name.indexOf('givenchar') - it gives the index of the given characater
   8.name.substring(int index) - substring with single parameter
                                  substring finds the sub of a string
                                  sub means partial
   for example ram singh we need only singh from the name we use
   substring
   from that point till end will be the answer 
   if we want last part then we can use single parameter
                  
  08.name.substring(int startindex,int endingindex)- substring with doubleparameter
     from that point - 1
   9.name.contains - return type is boolean
  10.name.matches - return type is boolean
  11.name.isEmpty - checks if the string is empty
  12.name.endsWith - checks if the string is ending with a particular characters or string
  13.name.replaceall - to replace a character or name
  14.name.repeat - it repeats the string for how many times we give
  15.name.split - LEARN THIS AGAIN
  16.name.equals   
     
     
     
     
data type of indexing is int
/*in string is also in follows indexing for example if we have
     String name = "good";
     g is in index 0
     o is in index 1
     o is in index 2
     d is in index 3
 In a string if there is a space also that is also has an index */   
     
     









